{
  "author": "DFST",
  "license": "Apache-2.0",
  "description": "MinaTokens Library",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @minatokens/api && npm run build -w @minatokens/storage && npm run build -w @minatokens/token && npm run build -w @minatokens/nft",
    "login": "npm login",
    "clean": "rm -rf node_modules && rm -rf packages/*/node_modules"
  }
}
